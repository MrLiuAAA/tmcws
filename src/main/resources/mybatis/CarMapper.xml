<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.qqd.dao.CarDao">

	<select id="findCarsByUserName" resultType="com.qqd.model.Car">
		SELECT t.*
		FROM g_car
		t
		WHERE t.username = #{username}
	</select>

	<select id="changeCarStatus" resultType="com.qqd.model.Car">
	update g_car g
	set g.alertstatus =#{alertstatus}
	where g.username =#{username} and g.sn =#{sn}
	</select>

</mapper>